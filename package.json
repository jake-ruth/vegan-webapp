{
  "name": "vegan-webapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node ./backend/dist/index.js && cd frontend && npm run build ",
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd backend && npm install && cd ../frontend && npm install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jake-ruth/vegan-webapp.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/jake-ruth/vegan-webapp/issues"
  },
  "homepage": "https://github.com/jake-ruth/vegan-webapp#readme",
  "devDependencies": {
    "concurrently": "^5.3.0"
  },
  "dependencies": {
    "dotenv-flow": "^3.2.0"
  }
}
