export interface RecipeFields {
  title: string;
  description: string;
  instructions: string;
  ingredients: string;
  prepHours: number;
  prepMinutes: number;
  cookHours: number;
  cookMinutes: number;
  yieldAmount: string;
}
